{
  "name": "demos",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch-api": "NODE_ENV=development parcel ./api/index.html --no-hmr --open --no-autoinstall",
    "watch-mnist": "NODE_ENV=development parcel ./mnist/index.html --no-hmr --open --no-autoinstall",
    "watch-mnist-internals": "NODE_ENV=development parcel ./mnist_internals/index.html --no-hmr --open --no-autoinstall",
    "build-demo": "NODE_ENV=production parcel build --no-minify --public-url ./"
  },
  "author": "",
  "license": "Apache-2.0",
  "devDependencies": {
    "@babel/core": "^7.0.0-0",
    "@babel/plugin-transform-runtime": "^7.1.0",
    "babel-preset-env": "~1.6.1",
    "parcel-bundler": "1.10.3",
    "rimraf": "^2.6.2"
  },
  "dependencies": {
    "@tensorflow/tfjs": "^0.12.7"
  }
}
