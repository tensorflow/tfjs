{
  "name": "tfjs-layers-demos",
  "version": "0.1.0",
  "description": "Demos for TensorFlow.js layers, not published",
  "private": true,
  "dependencies": {
    "deeplearn": "next",
    "pacman": "nsthorat/pacman",
    "tfjs-layers": "file:../dist/"
  },
  "scripts": {
    "prep": "rimraf node_modules/ && yarn",
    "test": "karma start"
  },
  "devDependencies": {
    "clang-format": "~1.2.2",
    "tslint": "~5.6.0",
    "tslint-no-circular-imports": "^0.2.1",
    "typescript": "2.4.2",
    "rimraf": "^2.6.2"
  }
}
